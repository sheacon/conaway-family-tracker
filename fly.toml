app = "where-are-mom-dad"
primary_region = "ord"

[build]

[env]
  DATABASE_URL = "sqlite:////data/app.db"
  FLASK_APP = "app:create_app"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"

[mounts]
  source = "data"
  destination = "/data"
